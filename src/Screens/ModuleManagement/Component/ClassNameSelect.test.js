import React from "react";
import { render } from "@testing-library/react";
import ClassNameSelect from "./ClassNameSelect";

describe("ClassNameSelect", () => {
  it("renders the component", () => {
    render(<ClassNameSelect />);
  });
});
